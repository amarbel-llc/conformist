# editors
.idea

# nix
result*
repl-result-*

# direnv
/.direnv
.env.local

# devshell
/.data

# goreleaser
dist/

# llm
.claude

# go
build/

# The eng template ships a starter .envrc for adopters; track it even though a
# parent/global ignore excludes .envrc generally (`nix flake init` copies it).
!/templates/eng/.envrc
