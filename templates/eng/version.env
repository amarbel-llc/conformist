# eng-versioning(7): the single source of truth for this repo's version. Rename
# EXAMPLE_VERSION to <YOUR_REPO>_VERSION (uppercase, `-`→`_`) and match the
# `linters.eng-versioning.key` in conformist.nix.
export EXAMPLE_VERSION=0.1.0
